# task_20260115

## 요청 요약
- ETL 실행 로그 저장 및 Settings에 최신 실행 시각 표시
- ECOS 10건 제한을 해제하고 거시지표를 풍부하게 적재
- DART 재무제표 적재 실패 원인 확인
- 변경사항(특히 DB 생성/DDL 수정) 기록

## 변경 내용
- ECOS KeyStatisticList를 적재하여 핵심 거시지표 다건 수집
- 기준금리 시계열은 별도 series_code로 유지
- DART 재무 적재 시 corp_code 미존재면 동기화 시도, announced_at 안정화
- DDL에 ingest_run_log 테이블 및 인덱스 추가

## DB 생성/DDL 수정
- `docs/postgres_erd_ddl_v2.2.sql`에 `ingest_run_log` 테이블 추가

## DART 재무 적재 실패 원인
- `company.corp_code`가 비어 있으면 대상이 0건으로 적재가 진행되지 않음
- `announced_at`을 NOW()로 넣으면 upsert가 깨져 중복 가능

## 배치 자동화 추가
- `scripts/etl_backfill_kis_prices.ps1`: 과거 가격 백필 (기본 180일)
- `scripts/etl_kis_prices_30min.ps1`: 전 종목 당일 가격 업데이트 (시간 단위 실행)
- `scripts/register_kis_prices_30min_task.ps1`: 윈도우 작업 스케줄러 등록

## 배치 스케줄 변경
- 과거 가격 백필 기본 180일
- 실시간 가격 적재는 09:00~16:00 사이 1시간 주기
