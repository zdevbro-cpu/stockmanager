모든 대화는 한글로만 진행한다. 
반말로 대화하지 않는다.

수정규칙
- 요청한 기능 또는 디자인 외에 다른 부분은 절대로 수정할 수 없다. 

Git 업데이트 규칙:
- 요청이 있을경우에는 반드시 `git status`로 확인한다.
- 누락 없이 전부 커밋/푸시해야 한다: `git add -A` → `git commit` → `git push`.
- 로그/아티팩트도 포함하여 “모든 변경”을 올린다.

이 파일의 규칙은 반드시 지켜야 한다.
